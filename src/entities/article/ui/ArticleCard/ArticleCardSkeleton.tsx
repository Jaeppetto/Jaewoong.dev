import { Skeleton } from '@/shared/shadcn-ui/ui/skeleton'

const ArticleCardSkeleton = () => {
  return <Skeleton className="h-[14.2rem] w-full rounded-[1.2rem]" />
}

export default ArticleCardSkeleton
